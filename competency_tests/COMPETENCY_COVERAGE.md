# Competency test coverage

Generated by `run_competency_checks.py`. Every number below comes from the run it describes, so this file cannot drift from what the checks actually found -- regenerate it rather than editing it.

- **Competency tests:** 28 of 28 evidenced by a passing check
- **Suite version:** ontology-quality-suite 0.14.2
- **Findings recorded:** 171 across 11 runs (`results/findings.csv`)

The definitions come from the five CSVs in this folder; the issue and summary text below is read from them at generation time rather than copied.

## Running it

```bash
uv sync                                              # suite 0.14.x, editable from ../../consolidated_ontology_suite_python
uv run python competency_tests/run_competency_checks.py   # regenerates this file
uv run pytest competency_tests -q                     # the same expectations as pass/fail tests
```

The run needs `oxi-gen` on the path or built in a sibling checkout: CT-2, CT-4, CT-13 and CT-15 to CT-22 are all asked of real triplified output, not of the query text. Each stage's own report is written verbatim to `results/`.

## What is in this folder

| Path | What it is |
|---|---|
| `*.csv` | the competency-test definitions, as supplied -- the source of the numbering used throughout |
| `fixtures/model/` | one worked example spanning all four layers: ontology 1.0.0 and 2.0.0, taxonomy, units, four TARQL mappings, the CSVs they read, and two comparable outputs |
| `fixtures/completeness/` | documentation-completeness defects, isolated so each is attributable |
| `fixtures/vsix/` | the dataset for the three checks only the VS Code extension implements |
| `checks/` | project-local `CMP-*` checks, one directory per graph they are asked of, plus their registry entries |
| `review_aids.py` | CT-23 to CT-28 -- comparisons between two outputs |
| `mapping_integrity.py` | CT-15 and CT-16 -- source records and defined mappings vs real output |
| `competency.py` | the coverage table: how each test is answered and what evidence proves it |
| `results/` | this run's findings and each stage's verbatim report |

Every seeded defect is marked in its fixture with an `# ERROR:` or `# SEEDS CT-n` comment naming the test it is there for.

## Coverage at a glance

| CT | Issue | How it is checked | Evidence |
|---|---|---|---|
| CT-1 | IRI generated by TARQL does not match the taxonomy/ontology concept IRI | registry + suite-module | `TQL-001` |
| CT-2 | Same IRI used for different concepts | project-check | `CMP-002` |
| CT-3 | IRI construction pattern not updated following a model change | suite-module | `rename-detected`, `undeclared-term` |
| CT-4 | taxonomy/ontology concepts referenced by TARQL do not exist | suite-module | `taxonomy-reference`, `taxonomy-membership` |
| CT-5 | Core concepts used inconsistently across domains | registry + harness | `TQL-001` |
| CT-6 | Ontology changes not propagated to TARQL mappings | suite-module + registry | `undeclared-term`, `CNF-001`, `CNF-002` |
| CT-7 | Changes not consistently reflected across dependent artefacts | suite-module | `major` |
| CT-8 | Missing concept definition | registry | `QUA-010` |
| CT-9 | Class has no place in the class hierarchy | project-check | `CMP-009`, `STR-004` |
| CT-10 | Missing preferred label | registry | `QUA-004`, `QUA-009` |
| CT-11 | Multiple preferred labels for the same language | registry | `QUA-009` |
| CT-12 | Labels or definitions declared in TARQL rather than the source model | project-check | `CMP-012` |
| CT-13 | Unexpected unused ontology terms | registry + project-check | `CNF-005`, `CMP-013` |
| CT-14 | Construct variable used but never defined | registry | `TQL-002`, `TQL-003` |
| CT-15 | Defined mapping produces no values | harness | `MAP-015` |
| CT-16 | Source-to-target population mismatch | harness | `MAP-016` |
| CT-17 | Required object property missing from a generated entity | project-check | `CMP-017` |
| CT-18 | Generated graph uses an unexpected modelling pattern | project-check | `CMP-018` |
| CT-19 | Magnitude missing a numeric value | project-check | `CMP-019` |
| CT-20 | Magnitude missing a unit of measure | registry | `DAT-004` |
| CT-21 | Unit of measure incompatible with the quantity | project-check | `CMP-021` |
| CT-22 | Numeric datatype or conversion error | registry | `TQL-005`, `CNF-004` |
| CT-23 | Prefix and namespace declarations differ between outputs | harness | `RVW-023` |
| CT-24 | Predicate usage differs unexpectedly between comparable outputs | harness | `RVW-024` |
| CT-25 | Class populations differ unexpectedly | harness | `RVW-025` |
| CT-26 | Subject-predicate values or relationships have changed | harness | `RVW-026` |
| CT-27 | Equivalent values appear different because of representation | harness | `RVW-027` |
| CT-28 | Identifiers described as global are inconsistent across outputs | harness | `RVW-028` |

## How each test is implemented

### CT-1 -- IRI generated by TARQL does not match the taxonomy/ontology concept IRI

*Check that IRIs constructed in TARQL BIND statements exactly match the corresponding IRIs declared in the taxonomy/ontology. Identify namespace, path, casing and identifier-pattern differences.* &middot; category: cross-artifact consistency

**Coverage:** registry + suite-module

**How to run it**

```
ontology-quality-suite sketch --queries fixtures/model/queries --file-pattern '**/*.rq'
ontology-quality-suite pattern-consistency --queries ... --ontology ... --taxonomy ...
```

**Seeded in:** `fixtures/model/queries/assets/sites.rq`, `fixtures/model/queries/readings/readings.rq`

TQL-001 compares BIND skeletons, so the same template fed by differently-named columns is not reported and a one-character divergence is. Namespace-level drift between a query's PREFIX block and the ontology is the prefix-alignment half of the same command.

**Observed**

- `TQL-001` x1 in run `sketch` -- ?site_IRI is bound in 3 query files by 2 structurally different expressions: IRI(CONCAT("https://example.org/water/data/site-", ?)) [assets.rq, sites...

### CT-2 -- Same IRI used for different concepts

*Identify cases where separate layers or datasets generate the same IRI for concepts with different meanings. Surface repeated construction patterns and their class or graph contexts for review.* &middot; category: cross-artifact consistency

**Coverage:** project-check

**How to run it**

```
CMP-002 over ontology + output data merged.
```

**Seeded in:** `fixtures/model/queries/assets/legacy_assets.rq`

TQL-001 answers the query-text half of this question. CMP-002 answers it of the real output, where a collision built from differently-named columns still shows.

**Observed**

- `CMP-002` x1 in run `project-output` -- IRI https://example.org/water/data/asset-A1 is typed both https://example.org/water/model#PumpStation and https://example.org/water/model#Reading, wh...

### CT-3 -- IRI construction pattern not updated following a model change

*Detect where an agreed IRI pattern has changed but TARQL BIND expressions still use the old pattern, particularly when concepts move into a shared or core namespace.* &middot; category: cross-artifact consistency

**Coverage:** suite-module

**How to run it**

```
ontology-quality-suite consistency --old water-v1.ttl --new water-v2.ttl --queries ...
```

**Seeded in:** `fixtures/model/ontology/water-v2.ttl`, `fixtures/model/queries/assets/assets.rq`

Rename detection pairs the removed and added terms, and the repair layer emits the substitution as a reviewable .patch against the query file.

**Observed**

- `rename-detected` x1 in run `consistency` -- https://example.org/water/model#PumpStation was renamed to https://example.org/water/model#PumpingStation (explicit dcterms:isReplacedBy assertion in...
- `undeclared-term` x1 in run `consistency` -- The mapping set builds https://example.org/water/model#PumpStation (class), which ontology 2.0.0 does not declare.

### CT-4 -- taxonomy/ontology concepts referenced by TARQL do not exist

*Check that every IRI generated within a taxonomy/ontology namespace corresponds to a concept present in that taxonomy/ontology, including misspelled or incorrectly constructed references.* &middot; category: cross-artifact consistency

**Coverage:** suite-module

**How to run it**

```
ontology-quality-suite pattern-consistency --queries ... --ontology ... --taxonomy ... --output-data <triplified output>
```

**Seeded in:** `fixtures/model/queries/assets/legacy_assets.rq`, `fixtures/model/queries/assets/assets.rq`, `fixtures/model/csv/assets.csv`

Two mechanisms, because a hard-coded reference and a per-row one are not findable the same way: the first is literal text in the query, the second only exists once the mapping has run.

**Observed**

- `taxonomy-reference` x1 in run `pattern-consistency` -- A query hard-codes https://example.org/water/vocab#Resevoir via https://w3id.org/semanticarts/ns/ontology/gist/isCategorizedBy, which the taxonomy ne...
- `taxonomy-membership` x1 in run `pattern-consistency` -- Real output references https://example.org/water/vocab#Culvert via https://w3id.org/semanticarts/ns/ontology/gist/isCategorizedBy, which the taxonomy...

### CT-5 -- Core concepts used inconsistently across domains

*Compare how shared or core concepts are used by different domain mappings. Surface significant differences in IRI construction, predicates, class assignment or mapping logic.* &middot; category: cross-artifact consistency

**Coverage:** registry + harness

**How to run it**

```
TQL-001 across both domain folders, then review_aids.compare_predicates between the two domains' outputs.
```

**Seeded in:** `fixtures/model/queries/assets/sites.rq`, `fixtures/model/queries/readings/readings.rq`

The shared concept here is :Site. The assets domain mints its IRI one way and the readings domain another, so the two domains' data never joins.

**Observed**

- `TQL-001` x1 in run `sketch` -- ?site_IRI is bound in 3 query files by 2 structurally different expressions: IRI(CONCAT("https://example.org/water/data/site-", ?)) [assets.rq, sites...

### CT-6 -- Ontology changes not propagated to TARQL mappings

*Check whether classes or properties renamed, replaced, deprecated or removed in an ontology are still referenced by TARQL constructs.* &middot; category: cross-artifact consistency

**Coverage:** suite-module + registry

**How to run it**

```
ontology-quality-suite consistency --old ... --new ... --queries ...; and sketch --ontology water-v2.ttl for the CNF-001/002 view.
```

**Seeded in:** `fixtures/model/queries/assets/assets.rq`, `fixtures/model/ontology/water-v2.ttl`

**Observed**

- `undeclared-term` x1 in run `consistency` -- The mapping set builds https://example.org/water/model#PumpStation (class), which ontology 2.0.0 does not declare.
- `CNF-001` x1 in run `sketch-v2` -- Class https://example.org/water/model#PumpStation is used with rdf:type in the sketch graph but is never declared owl:Class/rdfs:Class in the ontolog...
- `CNF-002` x1 in run `sketch-v2` -- Property https://example.org/water/model#hasFlowRate is used in the sketch graph but is never declared as a property in the ontology.

### CT-7 -- Changes not consistently reflected across dependent artefacts

*Compare versions of ontologies, taxonomies and mappings to identify changes made in one artefact but not propagated to the others.* &middot; category: cross-artifact consistency

**Coverage:** suite-module

**How to run it**

```
ontology-quality-suite version-diff old new, run once per artefact pair, then compare the bump each reports.
```

**Seeded in:** `fixtures/model/ontology/water-v1.ttl`, `fixtures/model/ontology/water-v2.ttl`

The ontology moved to 2.0.0 and the mappings did not move at all. Change is detected per artefact; noticing that only one artefact moved is the orchestration this harness adds.

**Observed**

- `major` x1 in run `version-diff` -- The ontology change implies a MAJOR bump. The mapping set carries no version of its own and was not changed at all.

### CT-8 -- Missing concept definition

*Check that required classes and concepts have an appropriate textual definition, such as skos:definition, according to the conventions for each artefact type.* &middot; category: completeness

**Coverage:** registry

**How to run it**

```
ontology-quality-suite checks --ontology fixtures/completeness/incomplete-model.ttl
```

**Seeded in:** `fixtures/completeness/incomplete-model.ttl`

QUA-010 asks for skos:definition specifically. STR-004 asks a different question -- whether the class is formally defined by an axiom -- and a term can satisfy one and fail the other.

**Observed**

- `QUA-010` x1 in run `completeness` -- Term must have at least one skos:definition.

### CT-9 -- Class has no place in the class hierarchy

*Flag locally defined classes that have no expected superclass or other required structural relationship, with appropriate exclusions for imported content.* &middot; category: completeness

**Coverage:** project-check

**How to run it**

```
CMP-009 over the ontology; STR-004 covers the weaker 'no defining axiom at all' case.
```

**Seeded in:** `fixtures/completeness/incomplete-model.ttl`

Imported vocabularies are excluded by namespace, which is the one line to edit per project.

**Observed**

- `CMP-009` x1 in run `completeness` -- Class https://example.org/water/completeness#Orphan has no rdfs:subClassOf and no owl:equivalentClass, so it sits outside the class hierarchy entirel...
- `STR-004` x1 in run `completeness` -- Class https://example.org/water/completeness#Orphan has no formal definition (no owl:equivalentClass, owl:intersectionOf, rdfs:subClassOf, owl:unionO...

### CT-10 -- Missing preferred label

*Check that resources requiring a label have an appropriate skos:prefLabel or accepted alternative. Extend existing checks beyond rdfs:label where needed.* &middot; category: completeness

**Coverage:** registry

**How to run it**

```
ontology-quality-suite checks --ontology fixtures/completeness/incomplete-model.ttl
```

**Seeded in:** `fixtures/completeness/incomplete-model.ttl`

QUA-004 accepts skos:prefLabel or rdfs:label; QUA-009 is the stricter form that requires a prefLabel specifically.

**Observed**

- `QUA-004` x1 in run `completeness` -- Resource https://example.org/water/completeness#Coupling has no skos:prefLabel or rdfs:label.
- `QUA-009` x1 in run `completeness` -- Term must have at least one skos:prefLabel, and at most one per language.

### CT-11 -- Multiple preferred labels for the same language

*Check that a concept has no more than one skos:prefLabel per language. Duplicates may indicate metadata declared in multiple artefacts.* &middot; category: completeness

**Coverage:** registry

**How to run it**

```
ontology-quality-suite checks --ontology fixtures/completeness/incomplete-model.ttl
```

**Seeded in:** `fixtures/completeness/incomplete-model.ttl`

Per language, not one overall: the fixture's Welsh prefLabel is correct SKOS and is not reported. The two English ones are.

**Observed**

- `QUA-009` x1 in run `completeness` -- Term must have at least one skos:prefLabel, and at most one per language.

### CT-12 -- Labels or definitions declared in TARQL rather than the source model

*Identify preferred labels and descriptions generated by TARQL when they should be maintained in the authoritative ontology or taxonomy/ontology.* &middot; category: completeness

**Coverage:** project-check

**How to run it**

```
CMP-012 over the CONSTRUCT-template sketch graph (out/sketch/sketch.ttl).
```

**Seeded in:** `fixtures/model/queries/assets/assets.rq`

**Observed**

- `CMP-012` x1 in run `project-sketch` -- The transformation mints http://www.w3.org/2004/02/skos/core#prefLabel for https://example.org/water/vocab#Reservoir, a term declared in the model ra...

### CT-13 -- Unexpected unused ontology terms

*Surface ontology classes or properties never used in the resulting graph, while distinguishing genuine omissions from intentionally unused or imported terms.* &middot; category: completeness

**Coverage:** registry + project-check

**How to run it**

```
CNF-005 for classes (data stage), CMP-013 for properties.
```

**Seeded in:** `fixtures/model/ontology/water-v1.ttl`

Both are Info: a term outside a given batch's scope is legitimately unused, and only a reviewer can separate that from a mapping nobody wrote.

**Observed**

- `CNF-005` x4 in run `data` -- Class http://www.w3.org/2002/07/owl#Thing is declared in the ontology but the data graph never uses it as an rdf:type.
- `CMP-013` x2 in run `project-output` -- Property https://example.org/water/model#hasReading is declared in the ontology but never used in the resulting graph. Confirm this is an intentional...

### CT-14 -- Construct variable used but never defined

*Detect variables referenced in a TARQL CONSTRUCT that are not bound, selected or otherwise defined in the WHERE clause.* &middot; category: TARQL and mapping integrity

**Coverage:** registry

**How to run it**

```
ontology-quality-suite sketch --queries fixtures/model/queries --file-pattern '**/*.rq'
```

**Seeded in:** `fixtures/model/queries/readings/readings.rq`, `fixtures/model/queries/readings/draft_alarms.rq`

Split by naming convention rather than by severity guesswork: a ?x_IRI variable is built rather than read, so an unbound one is a Violation; anything else is probably a CSV column and is Info.

**Observed**

- `TQL-002` x2 in run `sketch` -- ?site_IRI is used in the CONSTRUCT template of draft_alarms.rq but is never bound by a BIND or matched in the WHERE clause. Its naming convention say...
- `TQL-003` x4 in run `sketch` -- ?flow is used in the CONSTRUCT template of legacy_assets.rq but is not bound in the query. This is ordinarily correct -- TARQL binds each CSV header ...

### CT-15 -- Defined mapping produces no values

*Check that mapped attributes expected to produce triples actually appear in the output, including values lost through invalid datatype conversion.* &middot; category: TARQL and mapping integrity

**Coverage:** harness

**How to run it**

```
mapping_integrity.check_mapping_output_coverage(sketch, output) -- every (class, predicate) pair the CONSTRUCT templates define, counted in the real output.
```

**Seeded in:** `fixtures/model/queries/readings/readings.rq`, `fixtures/model/csv/readings.csv`

Predicate-level comparison is not enough: gist:hasUnitOfMeasure is present in the output on pressures and absent on every flow, so only the pair (class, predicate) shows the loss.

**Observed**

- `MAP-015` x2 in run `mapping-integrity` -- The transformation defines https://w3id.org/semanticarts/ns/ontology/gist/hasUnitOfMeasure on https://example.org/water/model#FlowRate, but none of t...

### CT-16 -- Source-to-target population mismatch

*Compare expected source records or populated source attributes with the resulting entities and properties, subject to explicit filtering rules.* &middot; category: TARQL and mapping integrity

**Coverage:** harness

**How to run it**

```
mapping_integrity.check_source_target_population(csv, output-per-file, class).
```

**Seeded in:** `fixtures/model/queries/readings/readings.rq`, `fixtures/model/csv/readings.csv`

Counted per output file rather than over the merged graph, so one mapping's collapse is not masked by another mapping's entities of the same class.

**Observed**

- `MAP-016` x1 in run `mapping-integrity` -- readings.csv has 6 data row(s) and, after 0 explicitly filtered, 6 https://example.org/water/model#Reading entit(y/ies) were expected; the output has...

### CT-17 -- Required object property missing from a generated entity

*Check that generated entities contain object-property relationships required by the agreed graph pattern.* &middot; category: TARQL and mapping integrity

**Coverage:** project-check

**How to run it**

```
CMP-017 over the output; the required (class, property) pairs are a VALUES table in the check.
```

**Seeded in:** `fixtures/model/queries/assets/assets.rq`, `fixtures/model/csv/assets.csv`

**Observed**

- `CMP-017` x1 in run `project-output` -- Generated https://example.org/water/model#PumpStation https://example.org/water/data/asset-A3 has no https://example.org/water/model#atSite link, whi...

### CT-18 -- Generated graph uses an unexpected modelling pattern

*Flag entities that conform syntactically but do not follow the agreed relationship pattern for their class or context, such as the expected magnitude structure.* &middot; category: TARQL and mapping integrity

**Coverage:** project-check

**How to run it**

```
CMP-018 over the output.
```

**Seeded in:** `fixtures/model/queries/assets/legacy_assets.rq`

pattern-consistency --dot renders the same question as a picture: the CONSTRUCT template coloured by which parts are verified against the model.

**Observed**

- `CMP-018` x2 in run `project-output` -- Entity https://example.org/water/data/asset-A1 carries gist:numericValue 120.5 directly, but is not a gist:Magnitude. The agreed pattern puts the num...

### CT-19 -- Magnitude missing a numeric value

*Check that each magnitude expected to represent a measurement contains its numeric value.* &middot; category: magnitudes, values and units

**Coverage:** project-check

**How to run it**

```
CMP-019 over the output.
```

**Seeded in:** `fixtures/model/queries/readings/readings.rq`, `fixtures/model/csv/readings.csv`

**Observed**

- `CMP-019` x1 in run `project-output` -- Magnitude https://example.org/water/data/pressure-S2-2026-01-05T120000 has no gist:numericValue. Usually an empty source cell that still built the ma...

### CT-20 -- Magnitude missing a unit of measure

*Check that each applicable magnitude has an associated unit of measure.* &middot; category: magnitudes, values and units

**Coverage:** registry

**How to run it**

```
ontology-quality-suite data <output> --ontology water-v1.ttl
```

**Seeded in:** `fixtures/model/queries/readings/readings.rq`

DAT-004 is pinned to gist's exact namespace and to gist:hasUnitOfMeasure. A graph on a pre-gist-12 vocabulary is not checked rather than checked and found wanting.

**Observed**

- `DAT-004` x6 in run `data` -- A gist:Magnitude must have a gist:hasUnitOfMeasure value typed gist:UnitOfMeasure.

### CT-21 -- Unit of measure incompatible with the quantity

*Validate that the unit makes sense for the type of measurement, using project-specific allowed-unit rules.* &middot; category: magnitudes, values and units

**Coverage:** project-check

**How to run it**

```
CMP-021 over the output; the allowed (magnitude class, unit) pairs are a VALUES table in the check.
```

**Seeded in:** `fixtures/model/queries/readings/readings.rq`, `fixtures/model/ontology/units.ttl`

**Observed**

- `CMP-021` x6 in run `project-output` -- Magnitude https://example.org/water/data/pressure-S1-2026-01-05T080000 is a https://example.org/water/model#Pressure measured in https://example.org/...

### CT-22 -- Numeric datatype or conversion error

*Detect values lost or altered because the mapping applies an invalid, unnecessary or incompatible datatype conversion.* &middot; category: magnitudes, values and units

**Coverage:** registry

**How to run it**

```
sketch for TQL-005, then data for the range violation the lost datatype causes.
```

**Seeded in:** `fixtures/model/queries/readings/readings.rq`

TQL-005 catches it in the query, before the pipeline runs. CNF-004 catches the consequence in the output. DAT-001 catches the third form -- a lexical form invalid for the datatype it does carry.

**Observed**

- `TQL-005` x1 in run `sketch` -- ?flowvalue_DT is named as a typed value but produces a String (readings.rq:44). The triple will load and carry an untyped string. Wrap the expression...
- `CNF-004` x13 in run `data` -- Value 1.8 of property https://w3id.org/semanticarts/ns/ontology/gist/numericValue doesn't match any of the property's declared range classes/datatype...

### CT-23 -- Prefix and namespace declarations differ between outputs

*Compare prefixes and namespace IRIs across artefacts, showing prefixes unique to one output and cases where one namespace uses different prefix names.* &middot; category: review aids

**Coverage:** harness

**How to run it**

```
review_aids.compare_prefixes(baseline, candidate)
```

**Seeded in:** `fixtures/outputs/baseline.ttl`, `fixtures/outputs/candidate.ttl`

**Observed**

- `RVW-023` x4 in run `review-aids` -- Prefix 'water:' (<https://example.org/water/model#>) is declared in the baseline only.

### CT-24 -- Predicate usage differs unexpectedly between comparable outputs

*Summarise predicates unique to each output, shared by both, or used differently so reviewers can focus on changed relationships.* &middot; category: review aids

**Coverage:** harness

**How to run it**

```
review_aids.compare_predicates(baseline, candidate)
```

**Seeded in:** `fixtures/outputs/baseline.ttl`, `fixtures/outputs/candidate.ttl`

**Observed**

- `RVW-024` x6 in run `review-aids` -- Predicate https://example.org/water/model#hasReading is used 2x in the baseline and never in the candidate.

### CT-25 -- Class populations differ unexpectedly

*Compare the number or presence of instances by class between comparable outputs and highlight substantial differences for investigation.* &middot; category: review aids

**Coverage:** harness

**How to run it**

```
review_aids.compare_class_populations(baseline, candidate)
```

**Seeded in:** `fixtures/outputs/baseline.ttl`, `fixtures/outputs/candidate.ttl`

**Observed**

- `RVW-025` x1 in run `review-aids` -- Class https://example.org/water/model#PumpStation has 4 instance(s) in the baseline and 3 in the candidate.

### CT-26 -- Subject-predicate values or relationships have changed

*For matching subject-predicate pairs, highlight changed object values and relationship targets without presenting every triple-level difference.* &middot; category: review aids

**Coverage:** harness

**How to run it**

```
review_aids.compare_subject_values(baseline, candidate)
```

**Seeded in:** `fixtures/outputs/baseline.ttl`, `fixtures/outputs/candidate.ttl`

**Observed**

- `RVW-026` x7 in run `review-aids` -- https://example.org/water/data/asset-A1 https://example.org/water/model#hasReading lost ['iri:https://example.org/water/data/reading-A1'].

### CT-27 -- Equivalent values appear different because of representation

*Normalise literals before comparison so datatype declarations, formatting or acceptable differences in geometric precision do not create false differences.* &middot; category: review aids

**Coverage:** harness

**How to run it**

```
review_aids.normalise, applied by CT-24 and CT-26 before comparing; reported as RVW-027, the list of differences it suppressed.
```

**Seeded in:** `fixtures/outputs/baseline.ttl`, `fixtures/outputs/candidate.ttl`

Reported as a finding even when it suppresses nothing, so a reviewer can see the normalisation ran rather than inferring it from an absence.

**Observed**

- `RVW-027` x1 in run `review-aids` -- Normalisation suppressed 2 representation-only difference(s) before comparing: https://example.org/water/data/reading-A1 https://w3id.org/semanticart...

### CT-28 -- Identifiers described as global are inconsistent across outputs

*Flag cases where the same real-world subject is assigned different identifiers despite an expectation of global stability.* &middot; category: review aids

**Coverage:** harness

**How to run it**

```
review_aids.compare_identifiers(baseline, candidate, identity_predicate=skos:notation)
```

**Seeded in:** `fixtures/outputs/baseline.ttl`, `fixtures/outputs/candidate.ttl`

Needs a business key to align on. Without one, a renamed subject is indistinguishable from a deletion plus an insertion.

**Observed**

- `RVW-028` x2 in run `review-aids` -- The subject identified by http://www.w3.org/2004/02/skos/core#notation "A3" is ['https://example.org/water/data/asset-A3'] in the baseline and ['http...

## Runs

| Run | What it covers | Findings | Command |
|---|---|---|---|
| `sketch` | Query source and shape vs ontology 1.0.0 | 18 | `ontology-quality-suite sketch --queries fixtures/model/queries --file-pattern '**/*.rq' --ontology fixtures/model/ontology/water-v1.ttl` |
| `sketch-v2` | Query shape vs ontology 2.0.0 (mappings not updated) | 22 | `ontology-quality-suite sketch --queries fixtures/model/queries --file-pattern '**/*.rq' --ontology fixtures/model/ontology/water-v2.ttl` |
| `data` | Triplified output vs ontology 1.0.0 | 61 | `ontology-quality-suite data <triplified output> fixtures/model/ontology/asset-types.ttl fixtures/model/ontology/units.ttl --ontology fixtures/model/ontology/water-v1.ttl` |
| `completeness` | Documentation completeness of an authored ontology | 10 | `ontology-quality-suite checks --ontology fixtures/completeness/incomplete-model.ttl` |
| `project-output` | Project-local checks over output + model | 14 | `run_competency_checks.py -> project_checks(output + ontology + taxonomy + units)` |
| `project-sketch` | Project-local checks over the CONSTRUCT-template sketch | 1 | `run_competency_checks.py -> project_checks(build_sketch_graph(queries))` |
| `pattern-consistency` | Taxonomy boundaries (query text and real output) | 14 | `ontology-quality-suite pattern-consistency --queries fixtures/model/queries --ontology fixtures/model/ontology/water-v1.ttl --taxonomy fixtures/model/ontology/asset-types.ttl --taxonomy fixtures/model/ontology/units.ttl --output-data <triplified output> --file-pattern '**/*.rq'` |
| `consistency` | Ontology 1.0.0 -> 2.0.0 vs the mappings | 6 | `ontology-quality-suite consistency --old fixtures/model/ontology/water-v1.ttl --new fixtures/model/ontology/water-v2.ttl --queries fixtures/model/queries --file-pattern '**/*.rq'` |
| `version-diff` | Semver bump implied by the ontology change | 1 | `ontology-quality-suite version-diff fixtures/model/ontology/water-v1.ttl fixtures/model/ontology/water-v2.ttl` |
| `mapping-integrity` | Source records and defined mappings vs real output | 3 | `run_competency_checks.py -> mapping_integrity.*` |
| `review-aids` | Baseline output vs candidate output | 21 | `run_competency_checks.py -> review_aids.compare_outputs(fixtures/model/outputs/baseline.ttl, fixtures/model/outputs/candidate.ttl)` |

### Run `sketch`

18 finding(s).

| Check | Severity | Count |
|---|---|---|
| `CNF-002` | Warning | 1 |
| `CNF-003` | Violation | 2 |
| `CNF-005` | Info | 6 |
| `TQL-001` | Warning | 1 |
| `TQL-002` | Violation | 2 |
| `TQL-003` | Info | 4 |
| `TQL-004` | Violation | 1 |
| `TQL-005` | Warning | 1 |

### Run `sketch-v2`

22 finding(s).

| Check | Severity | Count |
|---|---|---|
| `CNF-001` | Warning | 1 |
| `CNF-002` | Warning | 2 |
| `CNF-003` | Violation | 3 |
| `CNF-005` | Info | 7 |
| `TQL-001` | Warning | 1 |
| `TQL-002` | Violation | 2 |
| `TQL-003` | Info | 4 |
| `TQL-004` | Violation | 1 |
| `TQL-005` | Warning | 1 |

### Run `data`

61 finding(s).

| Check | Severity | Count |
|---|---|---|
| `CNF-003` | Violation | 2 |
| `CNF-004` | Violation | 13 |
| `CNF-005` | Info | 4 |
| `DAT-002` | Warning | 3 |
| `DAT-004` | Violation | 6 |
| `QUA-004` | Warning | 25 |
| `QUA-009` | Warning | 1 |
| `QUA-010` | Warning | 1 |
| `STR-004` | Warning | 3 |
| `STR-006` | Violation | 3 |

### Run `completeness`

10 finding(s).

| Check | Severity | Count |
|---|---|---|
| `CMP-009` | Warning | 1 |
| `QUA-001` | Warning | 1 |
| `QUA-004` | Warning | 1 |
| `QUA-009` | Warning | 3 |
| `QUA-010` | Warning | 1 |
| `STR-004` | Warning | 1 |
| `STY-004` | Warning | 2 |

### Run `project-output`

14 finding(s).

| Check | Severity | Count |
|---|---|---|
| `CMP-002` | Violation | 2 |
| `CMP-013` | Info | 2 |
| `CMP-017` | Violation | 1 |
| `CMP-018` | Violation | 2 |
| `CMP-019` | Violation | 1 |
| `CMP-021` | Violation | 6 |

### Run `project-sketch`

1 finding(s).

| Check | Severity | Count |
|---|---|---|
| `CMP-012` | Warning | 1 |

### Run `pattern-consistency`

14 finding(s).

| Check | Severity | Count |
|---|---|---|
| `taxonomy-membership` | Violation | 2 |
| `taxonomy-reference` | Violation | 12 |

Full report: `results/pattern-consistency.txt`

### Run `consistency`

6 finding(s).

| Check | Severity | Count |
|---|---|---|
| `rename-detected` | Warning | 2 |
| `repair-suggested` | Info | 1 |
| `undeclared-term` | Violation | 3 |

Full report: `results/consistency.txt`

### Run `version-diff`

1 finding(s).

| Check | Severity | Count |
|---|---|---|
| `major` | Warning | 1 |

Full report: `results/version-diff.txt`

### Run `mapping-integrity`

3 finding(s).

| Check | Severity | Count |
|---|---|---|
| `MAP-015` | Violation | 2 |
| `MAP-016` | Violation | 1 |

### Run `review-aids`

21 finding(s).

| Check | Severity | Count |
|---|---|---|
| `RVW-023` | Info | 3 |
| `RVW-023` | Warning | 1 |
| `RVW-024` | Info | 3 |
| `RVW-024` | Warning | 3 |
| `RVW-025` | Warning | 1 |
| `RVW-026` | Warning | 7 |
| `RVW-027` | Info | 1 |
| `RVW-028` | Info | 1 |
| `RVW-028` | Violation | 1 |

## Observations from building this

**`check_taxonomy_references` over-reports when a query declares its own `:` prefix.** Of the 12 taxonomy-reference finding(s) in run `pattern-consistency`, 11 name a CONSTRUCT *variable* rather than a hard-coded term: `note`, `site_IRI`, `flow_IRI`, `pressure_IRI`, `sitename`, `taken`, .... The check means to skip per-row entities, and recognises them by the sketch's scratch namespace (`https://tarqlviz.org/`). But `sketch.ttl` renders those entities with the *query's* own empty prefix whenever the query declares one -- all four mappings here declare `prefix : <https://example.org/water/model#>` -- so every unbound variable lands in the model's namespace and is reported as a nonexistent taxonomy reference. The one genuine finding, `vocab:Resevoir`, is outnumbered 11-to-1. The suite's own `examples/pattern_consistency/transform.rq` does not declare `:`, which is why its worked example shows a single clean finding.

**A two-graph question needs two graphs kept apart.** CMP-012 asks whether the *transformation* states a label for a term the *model* owns. Its first form copied the scratch-namespace filter above: that returns the right answer on these fixtures, but by accident -- a per-row entity the mapping labels would be missed for exactly the reason the note above describes. Merging the model into the sketch to fix it took the count from 1 to 101, because a single merged graph cannot say which file a triple came from and every term the ontology correctly labels then matched. What works is narrower on both sides: identify a model-owned term by what it is *declared* as, and merge in the model's `rdf:type` and `rdfs:subClassOf` triples only, leaving its labels out.

**Reference vocabularies have to be loaded for the checks that depend on them.** Run `data` without `asset-types.ttl` and `units.ttl` and DAT-004 reports 12 findings rather than 6: every `gist:hasUnitOfMeasure` link resolves to an untyped IRI, so the six correct pressure units are reported alongside the six genuinely missing flow units. The check is right both times; only one of the two inputs describes the system being assessed.

## Checks only the VS Code extension can run

Three ids are declared in the shipped `registry.json` and have no implementation in the Python package -- no `.rq` file and no SHACL shape. `docs/CHECKS.md` marks each of them *Not available in the CLI*; they are computed by the Ontology Development Suite extension (`consolidated_ontology_suite_webapp`, `ontology-dev-suite`).

| Check | Title | Implemented in |
|---|---|---|
| `REA-005` | Individual both owl:sameAs and owl:differentFrom the same node | `src/checks/reasoningRunner.ts` |
| `REA-006` | Reasoner-derived contradiction (unclassified) | `src/checks/reasoningRunner.ts` |
| `VOC-001` | Undeclared term referenced by an axiom | `src/checks/vocabularyChecks.ts` |

None of the 28 competency tests *depends* on these three: every one is evidenced by a check the CLI runs, as the table above records. They matter as a limit on what a clean CLI result means -- `VOC-001` in particular is the one referential-integrity question SHACL's open-world semantics cannot ask, so an axiom pointing at a misspelled class is invisible to every check the CLI has.

`fixtures/vsix/extension-only.ttl` is a small dataset seeded for them:

1. Open `fixtures/vsix/extension-only.ttl` in VS Code with the extension installed (`code --install-extension consolidated_ontology_suite_webapp/ontology-dev-suite-0.13.6.vsix`).
2. Run **Ontology Suite: Run Local Checks** from the command palette.
3. The Problems panel should report `VOC-001` on `:Pump rdfs:subClassOf :Machnie` (a typo for `:Machine`) and `REA-005` on `:pump-17`, which is asserted both `owl:sameAs` and `owl:differentFrom` `:pump-seventeen`.

For contrast, run the CLI over the same file:

```
uv run ontology-quality-suite checks --ontology competency_tests/fixtures/vsix/extension-only.ttl \
  --out-dir out/competency/vsix
```

Observed: 2 finding(s), check ids `DAT-002`, `QUA-004` -- and none of the three extension-only ids. The `:Machnie` typo and the sameAs/differentFrom contradiction both go unreported.

`REA-006` is a catch-all for a contradiction the extension's reasoner labelled with a reason its reader does not render specifically. It cannot be seeded deliberately -- firing at all means the ruleset has grown a case its reader has not -- so no fixture claims to trigger it.

